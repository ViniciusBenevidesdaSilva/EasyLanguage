programa

  numero fixedCost, variableCost, priceSale, result;
  
  leia(fixedCost);
  leia(variableCost);
  leia(priceSale);
  
  PEC(fixedCost, variableCost, priceSale);
  result = PEC(fixedCost, variableCost, priceSale);

  escreva(result);

fimprog;